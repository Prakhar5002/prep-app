/* Practice challenges — RN Deep Dives: Navigation (React Navigation v7 / Expo Router) */
(function () {
  var reg = window.PREP_SITE.registerChallenge;

  // ────────────────────────────────────────────────────────────
  reg({
    id: 'rn-nav-native-stack-vs-js-stack', track: 'rn', category: 'rn-navigation',
    difficulty: 'senior', type: 'deep-dive',
    prompt: 'React Navigation v7 ships two different stack implementations — native-stack and the plain JS stack. Architecturally, what\'s actually different between them, and why is native-stack the correct default in 2026?',
    answer: {
      core: `Native-stack renders <strong>real platform navigation controllers</strong> — <code>UINavigationController</code> on iOS, a <code>Fragment</code>-based equivalent on Android, via <code>react-native-screens</code> — so transitions, gestures, and screen lifecycle are handled by the OS itself. The JS stack instead keeps every screen inside one big RN view hierarchy and reimplements transitions/gestures in JS via Animated and gesture-handler. Native-stack is the documented default because performance and gesture fidelity come from the platform, not from JS re-deriving what the OS already does correctly.`,
      mechanism: `Each native-stack screen becomes a genuine native <code>ViewController</code>/<code>Fragment</code> that the OS manages directly — offscreen/onscreen lifecycle, memory pressure handling, native edge-swipe-to-go-back — instead of everything living inside a single React tree that JS has to animate by hand. The JS stack's swipe-to-dismiss, by contrast, must round-trip every frame of that gesture through JS/Reanimated rather than being handled natively by UIKit's or a Fragment transaction's own gesture recognizer. Because native-stack screens are true platform natives, features like large titles, form-sheet presentation, and native header search bars are only available (or only fully correct) on native-stack — they're OS APIs being surfaced through screen options, not something React Navigation reimplements in JS.`,
      tradeoffs: `The JS stack still exists for the narrow case of needing an arbitrary custom transition curve that native-stack's curated set of native transitions (<code>default</code>, <code>slide_from_right</code>, <code>fade</code>, <code>flip</code>, …) doesn't expose — you can't hand native-stack an arbitrary worklet-driven transition function the way the JS stack allows. In 2026 this is a deliberate, rare escape hatch rather than a coin-flip default.`,
      followups: [
        { q: 'If native-stack is backed by real UIViewControllers, what happens to component state/lifecycle when a screen goes off-screen but stays on the stack?', a: 'The React component stays mounted by default, so state is preserved — but the underlying native view can be told to stop re-rendering while off-screen via freezeOnBlur, or fully unmounted (discarding state) via unmountOnBlur, if you want to trade instant switch-back for lower steady-state cost.' },
        { q: 'Is native-stack mandatory in React Navigation v7 the way New Architecture is mandatory in RN 0.82?', a: 'No — the JS stack remains available and isn\'t deprecated, but native-stack has been the documented default recommendation for years now, and choosing the JS stack is treated as an intentional, justified escape hatch rather than a neutral alternative.' }
      ],
      redFlags: `Calling native-stack "just a faster JS stack" (it's an architecturally different renderer using real native controllers, not a JS reimplementation); assuming native-stack can express arbitrary custom transitions out of the box (it's limited to the platform's curated transition set unless paired with experimental Reanimated-based shared/custom transitions).`
    }
  });

  // ────────────────────────────────────────────────────────────
  reg({
    id: 'rn-nav-expo-router-vs-react-navigation', track: 'rn', category: 'rn-navigation',
    difficulty: 'senior', type: 'deep-dive',
    prompt: 'Expo Router is file-based and conceptually derives from React Navigation\'s internals. What are you actually trading away — or gaining — by picking Expo Router over hand-rolled React Navigation, and when would you still choose the latter?',
    answer: {
      core: `Expo Router still works the same way conceptually — it generates a navigator tree from your <code>app/</code> directory structure and derives deep-linking config automatically from file paths — but <strong>as of Expo SDK 56</strong> (stable, May 2026) it is no longer a thin wrapper over the public <code>react-navigation</code> package. Expo Router forked its own copy of React Navigation's internals, so an app built on SDK 56+ no longer depends on <code>@react-navigation/*</code> as a runtime package at all; that internal fork is what Expo Router now ships and controls directly. You still gain convention-driven routing, typed routes, and app/web/deep-link parity for free; you give up some fine-grained imperative control over how the navigator tree is assembled, and — since the fork — direct access to React Navigation's own package exports, since the equivalent hooks/components now have to come from Expo Router itself.`,
      mechanism: `File-naming conventions do the work: <code>app/index.tsx</code> is the root screen, <code>app/profile/[id].tsx</code> is a dynamic segment capturing <code>id</code> as a param, <code>app/(tabs)/</code> is a parenthesized <em>group</em> that doesn't appear in the resulting URL at all — it exists purely to apply a shared layout/navigator to its children. A <code>_layout.tsx</code> file in a directory defines the navigator (<code>Stack</code>, <code>Tabs</code>, etc.) for everything inside it. Because this file tree is the single source of truth, Expo Router derives the linking config (paths, param names) automatically — a URL like <code>/profile/42</code> deep-links correctly without you hand-writing the <code>linking</code> config object that plain React Navigation requires you to maintain yourself.`,
      tradeoffs: `Apps needing a dynamic, data-driven navigator structure decided at runtime (which set of tabs to render depends on a remote feature flag resolved after login) can find Expo Router's static file-tree model awkward — you lean on declarative guards like <code>Stack.Protected</code>, which are still bound to a fixed file tree, or drop into more imperative React Navigation composition for the genuinely dynamic part. Teams already deeply invested in an existing React Navigation tree, or needing exotic imperative navigator composition, can reasonably skip Expo Router; new projects — especially ones also targeting web — gain the most from adopting it.`,
      followups: [
        { q: 'Does using Expo Router mean you lose access to React Navigation APIs like useNavigation or navigation.setParams?', a: 'The capability doesn\'t disappear, but as of Expo SDK 56 the import path does: Expo Router forked its own copy of React Navigation\'s internals, so importing useNavigation, useRoute, setParams, etc. directly from @react-navigation/* in app code now breaks. The equivalent hooks still exist and keep working, but must be imported from expo-router (or expo-router/react-navigation) instead; Expo ships a codemod to rewrite existing @react-navigation/* imports automatically. Router\'s own hooks (useRouter, useLocalSearchParams) remain the more ergonomic option on top of that.' },
        { q: 'How does a group folder like app/(tabs)/ differ from a dynamic segment like app/[id]/ in the resulting URL?', a: 'A parenthesized group never appears in the URL at all — it\'s purely organizational, applying a shared layout/navigator to its children — while a bracketed segment is a real URL path parameter that gets captured into the route\'s params.' }
      ],
      redFlags: `Describing Expo Router as "a replacement for React Navigation" (it's a routing layer built on React-Navigation-derived internals, not a different navigation runtime); calling it "a thin wrapper over the public react-navigation package" without qualification — that was true before Expo SDK 56, but as of SDK 56 it's a decoupled fork that no longer depends on that package; assuming direct @react-navigation/* imports still work in app code post-SDK-56 (they break — the equivalent hooks/components now have to come from expo-router itself); assuming file-based routing means giving up React Navigation's imperative APIs entirely (the capability remains, just re-exported through a different import path).`
    }
  });

  // ────────────────────────────────────────────────────────────
  reg({
    id: 'rn-nav-deep-linking', track: 'rn', category: 'rn-navigation',
    difficulty: 'senior', type: 'deep-dive',
    prompt: 'Walk through what actually has to be true — app config plus native platform config — for a URL like https://app.example.com/orders/42 to open your app directly to the order-detail screen instead of a browser tab.',
    answer: {
      core: `Three layers all have to agree. First, the OS must know your app claims that domain: <strong>universal links</strong> on iOS via an <code>apple-app-site-association</code> file served from the domain plus an Associated Domains entitlement, and <strong>App Links</strong> on Android via an <code>assetlinks.json</code> file served from the domain plus an <code>autoVerify</code> intent-filter. Second, React Navigation's <code>linking</code> config (or Expo Router's auto-derived config) needs a matching <code>prefix</code>. Third, a path-to-screen mapping turns <code>/orders/42</code> into <code>{ screen: 'OrderDetail', params: { id: '42' } }</code>.`,
      mechanism: `<code>apple-app-site-association</code> is served at <code>/.well-known/apple-app-site-association</code> over plain https with no redirect, referencing your Team ID/App ID; iOS validates it at install/update time, so a broken or missing file just means links silently open the website instead of the app. The Android equivalent is <code>/.well-known/assetlinks.json</code> plus <code>&lt;intent-filter android:autoVerify="true"&gt;</code> declaring the domain. On the RN side:
<pre><code class="language-js">linking={{
  prefixes: ['https://app.example.com', 'myapp://'],
  config: { screens: { OrderDetail: 'orders/:id' } }
}}</code></pre>
Registering both the custom scheme (<code>myapp://</code>) and the https universal-link prefix in the same config is normal — a real app almost always needs both. React Navigation calls <code>getStateFromPath</code> internally to turn the incoming path into a navigation state tree matching this config; you can override that function entirely for exotic path-matching needs.`,
      tradeoffs: `Broken association files are a common silent-failure mode — the link "kind of works" (opens the browser, or opens the app to the wrong or blank screen) rather than throwing an obvious error, because there's no dev-time validation loop the way there is for, say, a broken import. Testing must include the real signed build path, since a debug build with a mismatched Team ID/bundle ID in the association file will silently fail verification while looking fine in every other respect.`,
      followups: [
        { q: 'A deep link opens the app but to the wrong screen — where do you look first?', a: 'Whether the incoming path actually matches the config screens mapping (a typo in the path pattern, or a mismatched param name) — before suspecting anything native, since that mapping is pure JS and is the easiest layer to unit-test directly via getStateFromPath.' },
        { q: 'Why do most production apps register both a custom scheme and universal links, rather than just one?', a: 'They serve different sources of incoming links, not the same purpose twice — the custom scheme is the reliable path for testing and for other apps/notifications constructing links directly, while universal/app links are what makes an ordinary web URL shared in Messages, email, or a QR code open the app instead of a browser tab.' }
      ],
      redFlags: `Treating the linking prop's prefixes/config alone as sufficient (it does nothing without the native association files actually being served and verified by the OS); assuming a deep-linking failure is a React Navigation bug before checking whether the native association files are even reachable or correctly formed.`
    }
  });

  // ────────────────────────────────────────────────────────────
  reg({
    id: 'rn-nav-state-persistence', track: 'rn', category: 'rn-navigation',
    difficulty: 'core', type: 'deep-dive',
    prompt: 'You want the app to reopen to whatever screen the user was last on, across a full kill-and-relaunch. What\'s the actual mechanism, and what\'s the most common way teams get it subtly wrong?',
    answer: {
      core: `<code>NavigationContainer</code> exposes <code>onStateChange</code> (fires on every navigation state change) to persist the current navigation tree, and <code>initialState</code> to rehydrate it on next launch — typically via <code>AsyncStorage</code>, read once before the container mounts. Because that read is async, the app must hold a loading placeholder until the persisted state is available; feeding React Navigation a stale or incompatible state is expected and handled gracefully, but shouldn't be relied on for anything beyond convenience.`,
      mechanism: `On launch: read the persisted JSON before rendering <code>NavigationContainer</code>, gate rendering behind an <code>isReady</code> flag, and pass the parsed object as <code>initialState</code> (or <code>undefined</code>, which falls back to the default state). Persist on every <code>onStateChange</code> call, typically straight to <code>AsyncStorage.setItem</code>. Because the state is serialized to JSON, params must contain only plain serializable data — no functions, class instances, or circular references — which is exactly why the params-vs-global-state boundary matters here specifically: an <code>onPress</code> callback passed as a param would silently vanish across a persist/restore round trip even though it works fine at runtime with persistence disabled.`,
      tradeoffs: `The most common mistake is treating persisted state as reliable across app version upgrades — a screen renamed or removed between releases means the persisted state can reference a route that no longer exists. React Navigation is tolerant of this (it drops unknown routes rather than crashing), but that tolerance surfaces as the user's restored tab silently changing or their history quietly truncating, not an obvious error — pair persistence with an error boundary that clears the persisted key on failure, rather than assuming it's a hard guarantee.`,
      followups: [
        { q: 'What should you NOT put in navigation params if you plan to persist state?', a: 'Anything non-serializable — functions/callbacks, class instances, unstringified Dates, circular references — since the persisted layer round-trips through JSON.stringify/parse; functions in particular silently vanish rather than throwing an error.' },
        { q: 'Should you persist navigation state for every kind of app?', a: 'No — it\'s most valuable for apps with deep, exploratory navigation the user would be annoyed to lose (a multi-step form, a shopping flow); for apps that always want a fresh start (most consumer apps\' bottom-tab home), skipping persistence and always booting to a known initial route is often the better default.' }
      ],
      redFlags: `Persisting state without gating render on the async read completing (produces a flash of default state, or a race where restored state gets overwritten); putting non-serializable data like callbacks in route params and expecting them to survive a persist/restore cycle.`
    }
  });

  // ────────────────────────────────────────────────────────────
  reg({
    id: 'rn-nav-params-vs-global-state', track: 'rn', category: 'rn-navigation',
    difficulty: 'core', type: 'deep-dive',
    prompt: 'A screen needs a piece of data. When does that data belong in route params versus a global store (Redux/Zustand/Context)? What breaks when a team gets this wrong in each direction?',
    answer: {
      core: `Route params are for values that identify or configure <em>this specific screen instance</em> — an id, a filter, a mode flag — conceptually part of that screen's "address," not an app-wide concern. Global state is for data that outlives any single screen's lifetime or is shared across unrelated parts of the tree. Overloading params with large or frequently-changing objects, or overloading a global store with screen-local UI state, are both real, recurring mistakes.`,
      mechanism: `The canonical correct pattern: <code>navigate('OrderDetail', { orderId: 42 })</code>, then the screen itself reads the full order object from a query/store keyed by <code>orderId</code>, rather than passing the whole order object as a param. This keeps the param serializable (which matters for deep linking and persistence — a URL can express <code>orderId=42</code>, not an entire object graph) and keeps the source of truth in one place, so if the order updates elsewhere the detail screen isn't left holding a stale snapshot. The opposite mistake — passing large objects or callbacks as params — creates two independent copies of the same fact (or breaks serialization, as above), and screen params are retained for the lifetime of that stack entry, so a large object param can quietly outlive its usefulness in memory.`,
      tradeoffs: `This isn't a hard rule — small, genuinely instance-scoped, ephemeral values (a "which tab this was opened from" analytics flag, a one-shot toast message id) are legitimately fine as params and would be overkill to route through global state. The real line is "does this describe which screen/variant this is" (param) versus "is this a shared fact about app/domain state" (store) — teams that reflexively push everything into a global store for consistency end up with that store cluttered by things that are really just this-screen-instance configuration, which is its own maintenance cost.`,
      followups: [
        { q: 'Why specifically does passing a whole object (not just its id) as a param hurt deep linking?', a: 'A URL can only represent primitive/serializable values in its path or query — an id round-trips through a URL trivially, but a full object graph structurally cannot, so a deep link built that way can never reconstruct the same navigate() call an in-app screen transition would produce.' },
        { q: 'Is it ever correct to pass a callback function as a param?', a: 'It works at runtime, since React Navigation doesn\'t serialize params held only in memory — but it silently breaks state persistence and can\'t be expressed by an incoming deep link, so most teams treat it as a smell and prefer lifting the callback\'s effect into a store/event, or having the destination screen report back via navigation.goBack() with a result.' }
      ],
      redFlags: `Passing whole fetched objects (rather than ids) as route params "for convenience"; storing screen-instance-scoped UI state (a form's draft, a toggle only that screen cares about) in a global store instead of local component state or params.`
    }
  });

  // ────────────────────────────────────────────────────────────
  reg({
    id: 'rn-nav-nested-type-safety', track: 'rn', category: 'rn-navigation',
    difficulty: 'senior', type: 'deep-dive',
    prompt: 'You have a bottom-tab navigator, each tab containing its own stack, and a modal stack over the whole thing. How do you get end-to-end TypeScript type safety for navigate() calls across that nesting, and where does it typically break down?',
    answer: {
      core: `React Navigation v7's static API infers a global <code>RootParamList</code> from your top-level navigator config via <code>StaticParamList</code> plus a <code>declare global</code> module augmentation, giving typed <code>navigate()</code>/<code>route.params</code> everywhere — but that inference is anchored at the root. A nested navigator's own screens aren't automatically visible to that global type unless the nested navigator is itself expressed through the same static config tree, not assembled separately.`,
      mechanism: `<pre><code class="language-ts">// RootNavigator.tsx
const RootStack = createNativeStackNavigator({
  screens: {
    Tabs: TabNavigator,   // itself a StaticParamList-based navigator
    Modal: ModalScreen,
  },
});

declare global {
  namespace ReactNavigation {
    interface RootParamList extends StaticParamList&lt;typeof RootStack&gt; {}
  }
}</code></pre>
Because <code>TabNavigator</code> is itself defined via the static config, its screens and params compose into the inferred <code>RootParamList</code> automatically — that composition is exactly what gives cross-navigator type safety. Things break down when a team mixes static and dynamic styles in the same tree (<code>createNativeStackNavigator&lt;MyParamList&gt;()</code> alongside static navigators elsewhere), or when a cross-navigator call jumps directly into a nested stack's specific screen (<code>navigate('Tabs', { screen: 'Profile', params: {...} })</code>) — that nested param list needs to be explicitly known to the type checker, not just correctly typed in isolation.`,
      tradeoffs: `The static API is genuinely less error-prone but more rigid — it wants the whole navigator tree assembled from one config object, which doesn't fit every use case where the set of screens depends on runtime state (a feature flag resolved after login). Teams doing that kind of exotic runtime-conditional composition often fall back to the dynamic API with hand-maintained param-list types, trading some type-inference convenience for flexibility — versus Expo Router's Stack.Protected-style declarative guards, which stay within a fixed file tree.`,
      followups: [
        { q: 'What\'s the actual runtime consequence if a nested navigate() call\'s target screen name is wrong, versus a TypeScript-level error?', a: 'When correctly wired, TypeScript catches an invalid screen name as a compile error before the app ever runs; without that wiring (or with an "any"-typed escape hatch somewhere in the chain), the failure only shows up at runtime as either a silent no-op or an "unhandled action" warning from the navigator that couldn\'t find the target route.' },
        { q: 'Do type-checked routes eliminate the need for deep-linking config, or vice versa?', a: 'No — they solve different problems (compile-time call-site correctness versus runtime URL-to-screen mapping), and in classic React Navigation both still have to be kept consistent with the actual navigator tree by hand; Expo Router\'s file-based approach is valuable specifically because the file structure drives both simultaneously instead of maintaining two separate hand-written sources of truth.' }
      ],
      redFlags: `Assuming typing only the root navigator automatically type-checks every nested screen reference (it doesn't, unless the nested navigator is itself part of the same static config tree); reaching for "as any" at a navigate() call site to silence a nesting-related type error instead of fixing the param-list composition.`
    }
  });

  // ────────────────────────────────────────────────────────────
  reg({
    id: 'rn-nav-auth-flows', track: 'rn', category: 'rn-navigation',
    difficulty: 'staff', type: 'deep-dive',
    prompt: 'Design the navigator structure for an app\'s authenticated versus unauthenticated flow — sign-in/sign-up screens versus the main app. What are the real architectural choices here, and what\'s the failure mode of the naive approach?',
    answer: {
      core: `The two real patterns are (a) conditionally rendering an entirely different root navigator tree based on auth state — "swap the whole tree" — and (b) a single tree with declarative per-branch guards (React Navigation's <code>UNSTABLE_routeNamesChangeBehavior</code>, or Expo Router's <code>Stack.Protected</code>). The naive mistake is guarding at the <em>screen render</em> level — checking <code>if (!user) return &lt;Redirect /&gt;</code> inside a component that's still mounted inside the authenticated navigator — rather than at the <em>navigator structure</em> level: that leaves the protected screen briefly reachable (and its route sitting in navigation history) before the redirect fires.`,
      mechanism: `Pattern (a): conditionally render <code>&lt;AuthStack /&gt;</code> or <code>&lt;AppStack /&gt;</code> as the sole child of <code>NavigationContainer</code> based on a boolean. When it flips, React unmounts one tree and mounts the other — clean, no reachable ghost history — but it discards any in-flight navigation state in that branch, and the auth check itself (a stored-token read) still has to resolve behind a loading gate before either tree renders. Pattern (b): Expo Router's <code>Stack.Protected guard={isSignedIn}</code> — when a guard flips from true to false, its screens' history entries are automatically pruned and the user is redirected to the nearest accessible ("anchor") route, so sign-out cleanly clears the authenticated stack's history without manual bookkeeping. React Navigation core's related mechanism sets <code>UNSTABLE_routeNamesChangeBehavior: 'lastUnhandled'</code> so an incoming deep link arriving mid auth-transition gets retried against the new tree once the swap settles, instead of being silently dropped because it resolved against the stale, about-to-unmount tree.`,
      tradeoffs: `Full-tree swapping is simpler to reason about but a worse fit for "log in, land back exactly where a deep link was trying to take you" flows, since swapping the whole tree discards transient state. Declarative guards preserve more continuity but are a newer pattern with documented edge cases (guard behavior with dynamic route segments has open bugs). Whichever pattern you pick, resolving the <em>initial</em> auth check before rendering any navigator is non-negotiable — screen-level guards bolted onto an already-mounted tree are the actual anti-pattern, not a stylistic preference.`,
      followups: [
        { q: 'A user deep-links to a protected screen while logged out. What should actually happen, mechanically?', a: 'The link should be captured, the user routed to sign-in, and — after successful auth — the original destination replayed, not silently discarded; that\'s exactly the gap UNSTABLE_routeNamesChangeBehavior: \'lastUnhandled\' targets in React Navigation v7, and what Stack.Protected\'s anchor-route redirect handles implicitly by design in Expo Router.' },
        { q: 'Why is checking auth state inside a screen component (rather than at the navigator-tree level) specifically dangerous, not just inelegant?', a: 'The screen is still mounted and its route still exists in navigation history for the brief window before the redirect effect runs — a fast back-gesture, a screenshot, or an accessibility tool can observe protected content that should never have been reachable, which is a real security/privacy defect, not just a UX rough edge.' }
      ],
      redFlags: `Gating protected content with an in-component conditional render/redirect instead of structuring the navigator tree around auth state; not accounting for what happens to an in-flight deep link during a sign-in/sign-out tree swap.`
    }
  });

  // ────────────────────────────────────────────────────────────
  reg({
    id: 'rn-nav-performance', track: 'rn', category: 'rn-navigation',
    difficulty: 'staff', type: 'deep-dive',
    prompt: 'A tab-based app feels sluggish switching tabs, and screens are visibly "stuck" mid-transition. Beyond "use native-stack," what specific navigation-layer settings and native mechanisms explain — and fix — this class of problem?',
    answer: {
      core: `Three distinct levers, often conflated: native-stack's platform-native transitions solve <em>transition smoothness</em> (the animation itself); <code>lazy</code> plus mounting strategy solves <em>how much work happens on first tab switch</em> (deferring an unopened tab's initial mount); <code>freezeOnBlur</code>/detach-on-blur strategies solve <em>ongoing cost of off-screen screens</em> (stopping background re-renders or timers from competing for the JS thread). A "stuck mid-transition" symptom specifically points at the JS thread being busy with something unrelated to the transition itself, since the native transition animation runs off the JS thread on native-stack regardless of JS load.`,
      mechanism: `<code>lazy: true</code> on tab/drawer navigators defers a tab's first mount until it's actually focused once — this helps first-switch cost, not steady-state cost. <code>freezeOnBlur: true</code> (a screen option backed by <code>react-native-screens</code>) stops a blurred screen's React tree from re-rendering while off-screen — relevant when an inactive screen has a subscription, interval, or animation still driving state updates it doesn't need while hidden. It has documented rough edges: combining it with certain bottom-tab animation types (e.g. <code>animation: 'shift'</code>) can silently disable the freeze, and specific react-native-screens versions have shipped a memory-leak/FPS-drop regression when paired with tab navigators — so it needs to be verified empirically against your exact RN/screens version, not assumed to always help. More aggressive detach/unmount-on-blur strategies go further and actually tear the screen down rather than freezing it, trading instant switch-back (remount cost) for lower steady-state memory/CPU.`,
      tradeoffs: `Aggressively freezing or detaching every inactive screen isn't free — a screen with genuinely important background behavior (an in-progress upload's progress bar, a live subscription driving a badge count) shouldn't be frozen just because it's off-screen. Freeze/detach settings need to be scoped per-screen based on whether the screen's off-screen state actually needs to keep running, not applied uniformly for a blanket win — and because freezeOnBlur has known regressions on specific versions, it should be profiled before shipping, not switched on by default.`,
      followups: [
        { q: 'If native-stack transitions run natively, why would a transition ever look stuck?', a: 'The native transition and the JS-driven screen content underneath it are separate concerns — the platform animates the view controller/fragment transition natively regardless of JS thread load, but if the destination screen\'s first paint depends on JS thread work that\'s still blocked (a slow initial render, a synchronous computation on mount), the result is a smooth native slide revealing a screen that isn\'t actually ready yet, which reads to the user as "stuck."' },
        { q: 'What would you check first, concretely, before reaching for freezeOnBlur?', a: 'Whether the offending screen has an interval, subscription, or animation actively running while off-screen at all (Reanimated, a WebSocket listener, a timer) — if nothing is running in the background, freezing it fixes nothing, and the actual bottleneck is elsewhere (heavy computation on the destination screen\'s mount, or an unrelated re-render storm).' }
      ],
      redFlags: `Reaching for freezeOnBlur/detach-on-blur as a first response without confirming the specific bottleneck, rather than profiling first; assuming native-stack alone guarantees no performance issues regardless of what the screens themselves are doing on the JS thread.`
    }
  });

  // ────────────────────────────────────────────────────────────
  reg({
    id: 'rn-nav-modals-transitions', track: 'rn', category: 'rn-navigation',
    difficulty: 'senior', type: 'deep-dive',
    prompt: 'How do you present a screen as a real modal (not just a stack push that looks like one) in React Navigation v7 — and separately, what\'s the actual state of shared-element/custom transitions in 2026? Is it safe to rely on for production?',
    answer: {
      core: `True modal presentation is the <code>presentation</code> screen option (<code>'modal'</code>, <code>'formSheet'</code>, <code>'transparentModal'</code>, etc.) on native-stack, which maps directly to the platform's real modal-presentation API — not a visual approximation built by animating a regular push. That distinction matters for platform-correct behavior like swipe-to-dismiss, backdrop dimming, and status-bar/safe-area handling. Shared-element transitions, by contrast, remain <strong>experimental</strong> in 2026: native-stack integrates with Reanimated's <code>sharedTransitionTag</code> mechanism, but it's explicitly documented as experimental and not recommended for production.`,
      mechanism: `<code>presentation: 'modal'</code> on a native-stack screen makes it slide up and cover the previous screen using the platform's real modal presentation, with swipe-to-dismiss handled natively; <code>'formSheet'</code> renders the native partial-height sheet (with detent configuration options) rather than a JS-drawn approximation. For shared-element transitions: assign the same <code>sharedTransitionTag</code> to two <code>Animated.View</code>s on the source and destination screens, and Reanimated matches them across the navigation, animating the shared element between its two positions/sizes. Customization is limited — <code>SharedTransition.custom()</code> only lets you tune duration and spring parameters, not supply an arbitrary custom animation function — and support differs by architecture: Reanimated 3 supports it on the old architecture, while Reanimated 4 supports it on Fabric only from 4.2.0 onward, and still behind a feature flag.`,
      tradeoffs: `For a genuinely native-feeling modal, <code>presentation: 'modal'</code>/<code>'formSheet'</code> on native-stack is the correct, production-safe default — a thin wrapper over a real platform API, not something to second-guess. Shared-element transitions are a different risk category: pitch them as polish you spike and validate against your exact RN/Reanimated/architecture combination, not something to commit to a roadmap as guaranteed-shippable, given the explicit experimental status and New-Architecture feature-flag gating.`,
      followups: [
        { q: 'If a team insists on shipping a shared-element transition in production today, what\'s the actual professional response?', a: 'Prototype it early against the exact RN/Reanimated/architecture combination the app ships on, timebox the spike, and keep a fallback (a plain cross-fade or native-stack default transition) ready to ship instead if the experimental feature regresses on an upcoming Reanimated/RN update — treat it as detachable polish, not a load-bearing feature.' },
        { q: 'What\'s the practical difference between presentation: \'modal\' and just pushing a screen with a slide-up animation option?', a: 'The former maps to the OS\'s real modal-presentation semantics — native swipe-to-dismiss, correct status-bar/safe-area handling, the previous screen staying mounted-but-covered underneath — while a plain push with a custom slide-up animation only mimics the visual motion and gets none of that native behavior for free.' }
      ],
      redFlags: `Describing shared-element transitions as production-ready/stable in 2026 (they're explicitly experimental, with real architecture-version gating); implementing a "modal" purely via a custom slide-up push animation and missing the native modal semantics (dismiss gesture, safe-area/status-bar handling) that presentation: 'modal' gets for free.`
    }
  });

})();
